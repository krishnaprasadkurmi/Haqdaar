import hospitals from '../../data/hospitals.json' with { type: 'json' };
import schemes from '../../data/schemes.json' with { type: 'json' };

export const hospitalsData = hospitals;
export const schemesData = schemes;
